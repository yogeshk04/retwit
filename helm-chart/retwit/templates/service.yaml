apiVersion: v1
kind: Service
metadata:
  name: {{ include "retwit.fullname" . }}
  labels:
    {{- include "retwit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retwit.selectorLabels" . | nindent 4 }}
